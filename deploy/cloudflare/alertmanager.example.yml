route:
  receiver: staging-owner
  group_by: [alertname]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: staging-owner
    webhook_configs:
      - url: https://alerts.invalid/replace-with-secret-routing-endpoint
        send_resolved: true
